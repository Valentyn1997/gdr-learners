# @package _global_
model:
  target:
    # Hparams
    hid_layers_out: 4
    noise_std_Y: 0.01   # noise regularization for outcomes, \sigma^2_y
    lr: 0.0001          # learning rate for the target flow (CNF), \eta_\text{T}
    batch_size: 64      # batch size for the target network, b_T