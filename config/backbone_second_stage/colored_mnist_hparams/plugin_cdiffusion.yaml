# @package _global_
model:
  target:

    diffusion_lat_dim: 700
    T: 300
    diffusion_hid_dim: 128
    noise_std_X: 0.0         # noise regularization for covariates, \sigma^2_x
    lr: 0.0001                # learning rate for the nuisance VAE, \eta_\text{N}
    batch_size: 64           # batch size for the nuisance network, b_N

